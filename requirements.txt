# Sickle Cell Detection (Research/Portfolio Project) - Python dependencies
#
# This file pins major/minor versions so the environment is reproducible.
# Recommended target: Google Colab (free GPU tier) - see docs/environment.md
# for why this project is not run on CPU-only compute.
#
# Install with: pip install -r requirements.txt

# --- Core deep learning ---
torch==2.5.1
torchvision==0.20.1

# --- Data handling / numerics ---
numpy==1.26.4
pandas==2.2.2
Pillow==10.4.0
scikit-learn==1.5.1
scikit-image==0.24.0

# --- Data acquisition ---
kaggle==2.2.4            # Kaggle API for dataset download (2.x required for the
                          # newer KAGGLE_API_TOKEN auth method; older 1.6.x only
                          # supports the legacy kaggle.json flow)
requests==2.32.3         # for fetching external (Zenodo) dataset

# --- Duplicate / near-duplicate detection ---
imagehash==4.3.1

# --- Visualization ---
matplotlib==3.9.1
seaborn==0.13.2

# --- Interpretability (Grad-CAM) ---
grad-cam==1.5.4

# --- Experiment tracking / progress ---
tqdm==4.66.4

# --- Reproducibility / notebook use ---
jupyter==1.0.0
ipywidgets==8.1.3
